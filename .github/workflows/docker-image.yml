name: Docker Build and Publish

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Build
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        run: echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }}
        env:
          DOCKERHUB_USERNAME: vaskonti
          DOCKERHUB_PASSWORD: dckr_pat_CRNe_PyIf8xvoxlFWVl4lXlU3kk

      - name: Build Docker image
        run: docker build -t Vaskonti/mdp-c1:latest src

      - name: Push Docker image to DockerHub
        run: docker push Vaskonti/mdp-c1:latest
